doctype html
html
  head
    include font_import.pug
    style
      include styles.css
  body

    div.top-section
      div.logo
      div.user-info.align-right
        if user
          |Logged in as #{user.first_name} (#{user.role})
          br
          form(action='/logout' method='post' enctype='multipart/form-data')
            a(onclick="this.closest('form').submit(); return false;") 👋 Sign out
        else
          a(href='/login') 🔑 Sign in 
          |or 
          a(href='/users/add') 📜 sign up!
          br
        |🛒 Cart: 0 pcs, 0.00 €
    
    block child
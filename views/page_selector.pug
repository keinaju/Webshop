div.page-selector
    div
        if prev_page_href
            a.rounded-button(href=prev_page_href) 👈 Previous page
    div.align-center
        |#{head_text}
    div.align-right
        if next_page_href
            a.rounded-button(href=next_page_href) 👉 Next page
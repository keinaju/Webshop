// const db = require('./db');

// module.exports = async function add_order(product, quantity, price_per_pc, instructions, customer) {
//     return await db.query('CALL add_order(?, ?, ?, ?, ?, ?);', [product, quantity, price_per_pc, 'new', instructions, customer]);
// };